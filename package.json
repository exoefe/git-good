{
  "name": "collage-workshop",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build_gallery.mjs",
    "prepare-site": "npm run build",
    "start": "npm run build && npx http-server ./ -p 8080"
  },
  "devDependencies": {}
}

